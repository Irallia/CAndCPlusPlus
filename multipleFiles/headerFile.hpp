// Header files never contain a main() function.
// Include your header file with #include.

# pragma once           // avoid including headers twice
#include <iostream>

void example_func()
{
    std::cout << "!!!";
}